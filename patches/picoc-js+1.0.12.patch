diff --git a/node_modules/picoc-js/dist/bundle.js b/node_modules/picoc-js/dist/bundle.js
index 0000000..1111111 100644
--- a/node_modules/picoc-js/dist/bundle.js
+++ b/node_modules/picoc-js/dist/bundle.js
@@ -5803,6 +5803,7 @@ function callMain(args) {
       quit_(1, e);
     }
   } finally {
+    try { if (FS.streams[1] && FS.streams[1].stream_ops && FS.streams[1].stream_ops.flush) FS.streams[1].stream_ops.flush(FS.streams[1]); if (FS.streams[2] && FS.streams[2].stream_ops && FS.streams[2].stream_ops.flush) FS.streams[2].stream_ops.flush(FS.streams[2]); } catch(e) {}
   }
 }
 
@@ -5923,10 +5924,13 @@ module.exports = PicocModule;
 });
 
 function runC(cprog, consoleWrite=null) {
-   const pc = picoc();
-   pc.onRuntimeInitialized = () => {
-      pc.runc(cprog, consoleWrite);
-   };
+   return new Promise((resolve) => {
+      const pc = picoc();
+      pc.onRuntimeInitialized = () => {
+         const res = pc.runc(cprog, consoleWrite);
+         resolve(res);
+      };
+   });
 }
 
 export { runC };
diff --git a/node_modules/picoc-js/dist/bundle.umd.js b/node_modules/picoc-js/dist/bundle.umd.js
index 0000000..2222222 100644
--- a/node_modules/picoc-js/dist/bundle.umd.js
+++ b/node_modules/picoc-js/dist/bundle.umd.js
@@ -5811,6 +5811,7 @@ function callMain(args) {
 	      quit_(1, e);
 	    }
 	  } finally {
+	    try { if (FS.streams[1] && FS.streams[1].stream_ops && FS.streams[1].stream_ops.flush) FS.streams[1].stream_ops.flush(FS.streams[1]); if (FS.streams[2] && FS.streams[2].stream_ops && FS.streams[2].stream_ops.flush) FS.streams[2].stream_ops.flush(FS.streams[2]); } catch(e) {}
 	  }
 	}
 
@@ -5931,10 +5932,13 @@ module.exports = PicocModule;
 	});
 
 	function runC(cprog, consoleWrite=null) {
-	   const pc = picoc();
-	   pc.onRuntimeInitialized = () => {
-	      pc.runc(cprog, consoleWrite);
-	   };
+	   return new Promise((resolve) => {
+	      const pc = picoc();
+	      pc.onRuntimeInitialized = () => {
+	         const res = pc.runc(cprog, consoleWrite);
+	         resolve(res);
+	      };
+	   });
 	}
 
 	exports.runC = runC;
